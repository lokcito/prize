<% layout('/layout/page') -%>
  <div class="row">
    <div class="col-12">
      <% if (prize) { %>
      <div class="card">     
        <div class="card-body ">
          Oops. En estos momentos existe un sorteo en curso.
          <br> Debe culminarse, para crear uno nuevo.
        </div>
      </div>
      <% } else {  %>
      <div class="card">
        <h5 class="card-header">Crear nuevo Sorteo</h5>
        
        <div class="card-body ">
          <form method="POST" action="/prizes/store">
            <div class="form-group">
              <label>Nombre de la Institución</label>
              <input type="text"  name="school" required maxlength="250" class="form-control">
            </div>
            <div class="form-group">
              <button type="submit" class="btn btn-primary">
                Crear
              </button>
            </div>
          </form>
        </div>
      </div>        
      <% } %>

    </div>
  </div>

